language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t spinningstar/docker-react -f Dockerfile.devL .

script:
  - docker run -e CI=true spinningstar/docker-react npm run test
